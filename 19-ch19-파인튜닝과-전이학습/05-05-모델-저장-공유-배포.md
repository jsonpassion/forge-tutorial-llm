세션 19.5 콘텐츠를 `output_session_19_5.md`에 생성했습니다.

**포함된 핵심 내용:**
- **save_pretrained**: safetensors 포맷, 파일 구조, 저장/로딩 대칭성

> 📊 **그림 2**: save_pretrained로 저장되는 파일 구조

```mermaid
graph TD
    ROOT["my-model/"] --> W["model.safetensors<br/>모델 가중치"]
    ROOT --> C["config.json<br/>모델 설정"]
    ROOT --> T["tokenizer.json<br/>토크나이저"]
    ROOT --> TV["tokenizer_config.json<br/>토크나이저 설정"]
    ROOT --> S["special_tokens_map.json<br/>특수 토큰"]
    ROOT --> V["vocab.txt<br/>어휘 사전"]
```

- **push_to_hub**: 3가지 업로드 방법(직접 호출, Trainer, CLI), 인증 설정

> 📊 **그림 3**: Hugging Face Hub 업로드 인증 및 전송 흐름

```mermaid
sequenceDiagram
    participant U as 사용자
    participant CLI as huggingface-cli
    participant HUB as HF Hub
    U->>CLI: huggingface-cli login
    CLI->>U: 토큰 입력 요청
    U->>CLI: Access Token 입력
    CLI->>CLI: ~/.cache/huggingface/token 저장
    U->>HUB: model.push_to_hub('repo-name')
    HUB->>HUB: 리포지토리 생성/업데이트
    HUB-->>U: URL 반환
```

- **모델 카드**: YAML 메타데이터 + 마크다운 본문 구조, ModelCard 클래스

> 📊 **그림 4**: 모델 카드(Model Card)의 구조

```mermaid
graph TD
    MC["README.md<br/>모델 카드"] --> YAML["YAML 메타데이터"]
    MC --> BODY["마크다운 본문"]
    YAML --> L["language: ko"]
    YAML --> T["tags: text-classification"]
    YAML --> M["metrics: accuracy"]
    YAML --> D["datasets: 학습 데이터"]
    BODY --> DESC["모델 설명"]
    BODY --> USE["사용법"]
    BODY --> LIMIT["한계점"]
    BODY --> TRAIN["학습 설정"]
```

- **Gradio**: `gr.Interface`, `from_pipeline`, `launch(share=True)`, Spaces 배포

> 📊 **그림 5**: Gradio 데모 구축 및 Spaces 배포 흐름

```mermaid
flowchart TD
    A["pipeline 로드"] --> B["gr.Interface 생성<br/>fn, inputs, outputs"]
    B --> C{"배포 방식"}
    C -->|로컬| D["launch()<br/>localhost:7860"]
    C -->|공유 링크| E["launch(share=True)<br/>임시 공개 URL"]
    C -->|Spaces| F["HF Spaces 배포<br/>영구 호스팅"]
    F --> G["app.py + requirements.txt<br/>Git push"]
```


**품질 체크:**
- Mermaid 다이어그램 5개 (flowchart, graph, sequenceDiagram 혼합)
- `run:python` + `output` 블록 2개
- 비유 4개 (레시피 노트, git push, 의약품 설명서, 시식 코너)
- 역사적 에피소드 2개 (Model Cards 논문, Gradio 탄생)
- 흔한 오해/팁 박스 5개
- 참고 자료 6개 (모두 검증된 URL)
- 내부 링크는 manifest 경로 정확히 사용